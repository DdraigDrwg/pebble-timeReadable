#pragma once

  // declare prototypes
  
static int battery_to_temp(int );
static  char *myStrtok(char *parseStr, char *splitChars);
static void split_string (char **inarray, char *delimiter, char *instring);
static int first_digit(int );
static int outsideRange(int in, int min, int max);
//static int weather_fail;
